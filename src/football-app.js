import { loadinglogo } from "./loading-logo/loading-logo"
import { matchCards } from "./match-cards/match-cards"




/**
 * 
 * @param {HTMLDivElement} element 
 * 
 */
export const footballApp = (element) => {

    // loadinglogo(element);
    matchCards(element);

}